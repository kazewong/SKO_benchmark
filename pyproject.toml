[project]
name = "highjump-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dvc>=3.62.0",
    "matplotlib>=3.10.5",
    "opencv-python>=4.12.0.88",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
